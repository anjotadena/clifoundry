
Create `CONTRIBUTING.md` (simple version):

```md
# Contributing

- Run `make fmt` and `make lint` before pushing.
- Add tests for new commands when possible.
- Keep commands small; push business logic into `services/`.